name: Optimize Images

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/imgopt.yml'
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'

jobs:
  imgopt:
    name: Optimize Images
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Convert to WebP
        id: convert
        run: |
          sudo apt-get -y install webp
          pip install webp-converter
          cd hud-resources
          webpc -q=75 --r | grep -q "NOT convert" | ! grep -q "Converted files count: 0"
      - name: Compress Images
        id: compress
        if: github.repository == 'mastercomfig/hud-db'
        uses: calibreapp/image-actions@main
        with:
          # The `GITHUB_TOKEN` is automatically generated by GitHub and scoped only to the repository that is currently running the action. By default, the action can’t update Pull Requests initiated from forked repositories.
          # See https://docs.github.com/en/actions/reference/authentication-in-a-workflow and https://help.github.com/en/articles/virtual-environments-for-github-actions#token-permissions
          githubToken: ${{ secrets.GITHUB_TOKEN }}
          compressOnly: true
      - name: Set outputs
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      - name: Create New Pull Request If Needed
        if: steps.compress.outputs.markdown != '' || steps.convert.outcome == 'success'
        uses: peter-evans/create-pull-request@v4
        with:
          title: Auto Optimize Images ${{ steps.vars.outputs.sha_short }}
          branch: imgopt
          branch-suffix: short-commit-hash
          commit-message: optimize images for ${{ steps.vars.outputs.sha_short }}
          body: ${{ steps.calibre.outputs.markdown }}
